# Exemplo de configuração de ambiente para o frontend (Create React App)
# Em desenvolvimento, o package.json já define proxy para http://localhost:8000
# e o axios usa baseURL '/api/'. Você normalmente NÃO precisa definir isso.
# Se desejar apontar para outra API, descomente abaixo:
# REACT_APP_API_BASE_URL=http://localhost:8000/api/
